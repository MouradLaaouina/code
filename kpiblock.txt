      
      // Animations au scroll désactivées (affichage direct des sections)
      
      // KPI: afficher les valeurs finales immédiatement (pas d'animation au scroll)
      document.querySelectorAll('.kpi-value').forEach(counter => {
        const target = parseInt(counter.getAttribute('data-target'));
        if (!isNaN(target)) {
          counter.textContent = target.toLocaleString() + '+';
        }
      });
      
      // Lightbox moved below with typewriter behavior
      // Smooth scroll for anchor links
      document.querySelectorAll('a[href^="#"]').forEach(anchor => {
        anchor.addEventListener('click', function (e) {
          e.preventDefault();
